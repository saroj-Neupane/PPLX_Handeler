# PPLX File Handler Requirements
# This project uses only Python standard library modules
# No external dependencies required

# Python 3.6+ recommended
# Standard library modules used:
# - tkinter (GUI)
# - xml.etree.ElementTree (XML parsing)
# - os, glob, json (file operations)
# - threading (background processing)
# - pathlib, datetime (utilities)

# If tkinter is not available on your system:
# On Ubuntu/Debian: sudo apt-get install python3-tk
# On CentOS/RHEL: sudo yum install tkinter
# On macOS: tkinter is included with Python
# On Windows: tkinter is included with Python

# Excel file reading
openpyxl>=3.0.0
pandas>=1.3.0 